# Project instructions

- When Codex creates a Git commit in this repository, push that commit to both configured remotes, `origin` and `github`, in the same task.
- Do not create a commit merely because files changed; commit only when the user requests it or the active task otherwise explicitly includes committing.
- The live ComfyUI installation is the directory junction `C:\Software\ComfyUI\custom_nodes\ComfyUI-MV-Director` targeting this repository. Preserve the junction; repository edits are deployed automatically and must not be copied over it.
